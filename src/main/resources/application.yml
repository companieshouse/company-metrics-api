logger:
  namespace: company-metrics-api

chs:
  kafka:
    api:
      endpoint: ${CHS_URL:localhost}
      key: ${CHS_API_KEY:chsApiKey}

springfox:
  documentation:
    auto-startup: false

mongodb:
  company-metrics:
    collection:
      name: ${COMPANY_METRICS_COLLECTION_NAME:company_metrics}
  charges:
    collection:
      name: ${CHARGES_CHARGES_COLLECTION_NAME:company_mortgages}
  appointments:
    collection:
      name: ${APPOINTMENTS_COLLECTION_NAME:appointments}

spring:
  jackson:
    default-property-inclusion: NON_NULL
  data:
    mongodb:
      metrics:
        uri: ${MONGODB_METRICS_URL:mongodb://localhost:27017/company_metrics}
        name: company_metrics
        collection: ${METRICS_COLLECTION_NAME:company_metrics}
      charges:
        uri: ${MONGODB_CHARGES_URL:mongodb://localhost:27017/company_mortgages}
        name: company_mortgages
        collection: ${CHARGES_COLLECTION_NAME:company_mortgages}
      appointments:
        uri: ${MONGODB_CHARGES_URL:mongodb://localhost:27017/appointments}
        name: appointments
        collection: ${APPOINTMENTS_COLLECTION_NAME:appointments}

management:
  endpoints:
    enabled-by-default: false
    web:
      base-path: /company-metrics-api
      path-mapping:
        health: healthcheck
  endpoint:
    health:
      show-details: never
      enabled: true
